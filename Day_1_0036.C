#include<stdio.h>
int main (){
    int side, area;
    printf("Enter length of the side: ");
    scanf("%d", &side);
    area = (side+side);
    printf("area of the square is %d", &area);

    return 0;
}